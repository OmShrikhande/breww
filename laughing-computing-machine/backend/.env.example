# Player API reads laughing-computing-machine/.env (parent folder).
# Do not create backend/.env — edit the parent .env instead.
#
# Run combined server from parent:
#   cd laughing-computing-machine && npm run dev
#
# Standalone player API (optional):
#   cd laughing-computing-machine/backend && npm run dev
