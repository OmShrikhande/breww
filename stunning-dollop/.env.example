# Admin Dashboard (stunning-dollop)
#
# Local dev: leave unset — Vite proxies /api → http://localhost:3000
# Vercel:    leave unset — vercel.json rewrites /api → Render backend
# Custom:    set full backend origin (no trailing slash; paths already include /api)
#
# VITE_API_BASE_URL=https://breww-ysqj.onrender.com
