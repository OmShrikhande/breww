# Admin Dashboard (stunning-dollop)
# The Vite dev proxy forwards /api → localhost:3000 (laughing-computing-machine),
# so no VITE_API_BASE_URL is needed for local development.

# Production: set this to your deployed backend URL
# VITE_API_BASE_URL=https://YOUR-BACKEND-NAME.onrender.com/api
